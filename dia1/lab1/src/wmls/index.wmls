extern function voIndex()
{
<<<<<<< HEAD

    extern function solicitarCodigo() {
        var tipo = "NUMERIC";
        var titulo = "Ingrese su código";
        var valorPorDefecto = "";
        var fila = 3;
        var minimo = 4;
        var maximo = 6;
        var mostrarError = 1;
        var logoError = "ERROR.BMP";

        var codigo = Dialogs.extendedPrompt(
            tipo,
            titulo,
            valorPorDefecto,
            fila,
            minimo,
            maximo,
            mostrarError,
            logoError
        );

        if (String.length(codigo) >= minimo) {
        Dialogs.display("Código ingresado: " + codigo, 5);
        } else {
        Dialogs.errorMessage("Error", "Código inválido", 2, 1, 1);
        }
    }
=======
    Dialogs.display(Dialogs.center("Hola Mundo",1),5);
    Dialogs.insertBotton(0,10,7,84,280,"ESTE ES UN BOTON",143);
    
    while(true){
    	var key = UTILS.getKey(60000);
    	Dialogs.display(Dialogs.center(String.toString(key),1),10);
    }
    
>>>>>>> 14424d5f852187315edc533d06bd905e4ebf500f
}

